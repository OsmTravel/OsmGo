import { FilterByKeyLblPipe } from './filter-by-key-lbl.pipe';

describe('FilterByKeyLblPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByKeyLblPipe();
    expect(pipe).toBeTruthy();
  });
});
