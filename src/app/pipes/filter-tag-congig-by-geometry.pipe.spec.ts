import { FilterTagCongigByGeometryPipe } from './filter-tag-congig-by-geometry.pipe';

describe('FilterTagCongigByGeometryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTagCongigByGeometryPipe();
    expect(pipe).toBeTruthy();
  });
});
