import { FilterByByGeometryTypePipe } from './filter-by-geometry-type.pipe';


describe('FilterByByGeometryTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByByGeometryTypePipe();
    expect(pipe).toBeTruthy();
  });
});
