import { FilterHiddenTagsPipe } from './filter-hidden-tags.pipe';

describe('FilterHiddenTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHiddenTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
