import { FilterBySearchablePipe } from './filter-by-searchable.pipe';

describe('FilterBySearchablePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySearchablePipe();
    expect(pipe).toBeTruthy();
  });
});
