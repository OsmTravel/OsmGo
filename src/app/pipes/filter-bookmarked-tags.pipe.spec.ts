import { FilterBookmarkedTagsPipe } from './filter-bookmarked-tags.pipe';

describe('FilterBookmarkedTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBookmarkedTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
